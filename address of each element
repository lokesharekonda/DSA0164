#include <iostream>
int main() {
    const int SIZE = 5;
    int array[SIZE] = {10, 20, 30, 40, 50};
    for (int i = 0; i < SIZE; ++i) {
        std::cout << "Address of array[" << i << "] = " << &array[i] << std::endl;
    }
    return 0;
}
